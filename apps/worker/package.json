{
  "name": "@semaphore/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@semaphore/asymmetry": "workspace:*",
    "@semaphore/protocol": "workspace:*",
    "@semaphore/seed": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260826.1",
    "wrangler": "^4.126.0"
  },
  "exports": {
    "./*": "./src/*.ts"
  }
}
