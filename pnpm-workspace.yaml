packages:
  - apps/*
  - packages/*
  - tests

# wrangler pulls these and both need postinstall scripts to place a binary.
# Listed explicitly so an install never prompts and CI stays non-interactive.
allowBuilds:
  esbuild: true
  workerd: true
