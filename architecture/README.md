# Diagrams

Two SVGs, embedded in [ARCHITECTURE.md](../ARCHITECTURE.md) and readable on their own.

| File | Shows |
|---|---|
| [system-diagram.svg](system-diagram.svg) | The agent, the browser, the worker, the Durable Object and D1 — where state lives and which way it flows. |
| [asymmetry-model.svg](asymmetry-model.svg) | The five channels, the two server-side projections, and the possible-worlds proof that checks the split is decisive rather than merely present. |

Both are hand-authored, dependency-free SVG: no diagramming tool, no generated output to go stale
against the code. Palette matches the game's own locked twenty colours.
