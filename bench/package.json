{
  "name": "@semaphore/bench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ablation": "npx tsx ablation.ts",
    "typecheck": "tsc --noEmit",
    "benchmark": "npx tsx harness.ts"
  },
  "devDependencies": {
    "@semaphore/asymmetry": "workspace:*",
    "@semaphore/protocol": "workspace:*",
    "@semaphore/seed": "workspace:*",
    "@semaphore/worker": "workspace:*"
  }
}
